Screenshot evidence for the downtown skyline fix.

Image-only branch, deliberately not merged.

Both frames are the same headless capture with the chunk streamer settled, so
neither is an unfinished city.
