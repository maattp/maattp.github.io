Screenshot evidence for the spawn seam fix.

Image-only branch, deliberately not merged.

Both frames are the same headless capture at the spawn, on foot, with the game's
own chase camera and the chunk streamer settled first -- so neither is an
unfinished city. The red line marks the column where the seam was measured.
