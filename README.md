Before/after screenshots for PR #343 (Auto graphics pass).

Image-only branch, deliberately not merged: it exists so the PR can show
pictures without adding a megabyte of JPEGs to the source history.

Both sets were captured by the SAME harness (`tools/beauty.mjs` at the head of
`auto-graphics`), the "before" set run against a worktree at `ba97034` — the
commit the branch starts from. Framing, sun direction and population seeding are
therefore identical between the two, which they would not be if the older
captures had simply been reused.
